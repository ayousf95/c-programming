#include <stdio.h>

int main(){

  int x = 0x02;
  int y, z = 0;

  right_shift = x >> 2;
  printf("The result of right shift is %x\n", y);

  left_shift = x << 1;
  printf("Left shift results in %x\n", z);
return 0;
}
